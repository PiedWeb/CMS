{% if mainImage.count > 0 %}
    <h4>Via l'illustration principale</h4>
    <ul>
        {% for page in mainImage %}
            {% block item %}
                <li><a href="/admin/page/{{ page.id }}/edit">{{ page.h1|default(page.title)|raw }}</a></li>
            {% endblock %}
        {% endfor %}
    </ul>
{% endif %}

{% if gallery.count > 0 %}
    <h4>Via une gallerie</h4>
    <ul>
        {% for page in gallery %}
            {% block item2 %}
                <li><a href="/admin/page/{{ page.id }}/edit">{{ page.h1|default(page.title)|raw }}</a></li>
            {% endblock %}
        {% endfor %}
    </ul>
{% endif %}

{% if content %}
    <h4>Via le contenu</h4>
    <ul>
        {% for page in content %}
            {% block item3 %}
                <li><a href="/admin/page/{{ page.id }}/edit">{{ page.h1|default(page.title)|raw }}</a></li>
            {% endblock %}
        {% endfor %}
    </ul>
{% endif %}
