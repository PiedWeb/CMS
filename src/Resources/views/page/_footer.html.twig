<style>
#footer {
    border-top: 2px solid var(--bg-dark);
    font-size: 0.7em;
    color: var(--color-light);
}
#footer a {
    text-decoration:none;
}
</style>
<footer id="footer">
    <div class="container">
        {% block footer %}
            {% include view('/component/alternate_links.html.twig') %}
        {% endblock %}

        {% block extra_footer %}
            <p>
                <a href="{{ homepage() }}" style="font-size:2.2em">{{ app_name }}</a>
            </p>
            <p>Design
                <a href="https://piedweb.com">Pied Web</a>.</p>
        {% endblock %}
    </div>
</footer>
